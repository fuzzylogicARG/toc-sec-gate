services:
  - type: web
    name: toc-secure-gate
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GATE_SECRET
        generateValue: true